name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps: 
      - name: "print greeting"
        run: |
            echo "Hello World"
            echo "Hello saidulu where are you going..!"
      - name: "Print goodbye"
        run: echo "Done bye..!"
